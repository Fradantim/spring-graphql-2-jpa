type Query {
    findBookById(id: ID): Book
}

type Book {
    id: ID!
    name: String
    isbn: String
    author: Person
    reviewers: [Person]
    quotes: [Quote]
}

type Person {
    id: ID!
    name: String!
}

type Quote {
    id: ID!
    text: String
}